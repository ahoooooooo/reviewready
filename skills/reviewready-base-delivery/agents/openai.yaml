interface:
  display_name: "ReviewReady Base Delivery"
  short_description: "Conservative delivery with relevant proof"
  default_prompt: "Use $reviewready-base-delivery with the conservative loop, worker canary, one-artifact reviewer packet, watchdog, and validated reviewerReadiness handoff."
