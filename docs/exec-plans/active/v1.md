# v1 execution plan

## Goal

Deliver a publishable Node 22+ CLI and Node 24 JavaScript GitHub Action that
implement the v1 product contract with deterministic behavior and a documented
security boundary.

## Delivery loop

For each vertical slice:

1. record acceptance examples;
2. add a failing focused test;
3. implement the smallest behavior;
4. run focused and full tests;
5. inject malformed and adversarial inputs;
6. fix defects and retain them as regression tests;
7. update public documentation and this decision log.

## Milestones

- [x] Product contract and architecture boundary.
- [x] Minimal project-local Codex baseline.
- [x] Toolchain and reproducible quality gate.
- [x] Closed policy schema and semantic validation.
- [x] Pure rule matcher and evidence engine.
- [x] Stable reports and CLI contracts.
- [x] GitHub event/API adapter and packaged Action.
- [x] Fault injection and security regression suite.
- [x] Example policy and maintainer onboarding docs.
- [x] Local clean-room install, pack, and release-candidate verification.
- [x] Public name, package coordinates, and repository metadata selected.
- [x] Public npm package, GitHub tags, Release, and registry install verification.
- [x] Add a tested package-content and privacy gate for the v1.0.2 recovery
      release.
- [x] Create or move public release tags after the verified registry release.

## Decision log

- 2026-07-22: Use TypeScript on Node 22+ for the CLI and Node 24 for the
  JavaScript Action so the CLI is usable across the active LTS line.
- 2026-07-22: Keep the engine platform-independent and free of I/O.
- 2026-07-22: Keep v1 deterministic and serverless; no model or database dependency.
- 2026-07-22: Read effective policy from the base commit and fail closed.
- 2026-07-22: Bootstrap Codex with a short map and workspace permissions; add hooks
  and skills only after a repeated workflow or failure justifies them.
- 2026-07-22: Do not claim AGM compatibility without a published interoperability
  contract and author agreement.
- 2026-07-22: Direct competitor ReviewGate validates the problem but owns general
  PR-shape heuristics; ReviewReady remains focused on repository-owned evidence
  obligations and will make no uniqueness claim.
- 2026-07-22: Support pull_request_review events and require workflow ordering
  after any checks named by policy.
- 2026-07-22: Publish under the aligned ReviewReady brand,
  `@ahoooooo/reviewready` npm package, and `ahoooooooo/reviewready` repository
  coordinates. npm rejected the unscoped name as too similar to `review-ready`;
  the scope preserves the product and CLI names without evading that safeguard.
- 2026-07-22: Escape untrusted Markdown control characters in one pass and
  HTML-encode ampersands and angle brackets so reports preserve literal text and
  static analysis can verify complete sanitization.
- 2026-07-22: Fully unpublish npm v1.0.0 and v1.0.1 after personal email metadata
  was found in registry history. Preserve the public GitHub project, recover as
  v1.0.2, and make npm's planned tarball pass an allowlist and sensitive-content
  audit before any future publication.
- 2026-08-10: Re-ran the complete quality gate after formatting normalization
  and the minimal lockfile security update: 79 tests passed, coverage thresholds
  were met, and npm audit reported zero vulnerabilities.
- 2026-08-10: Published `@ahoooooo/reviewready@1.0.2` after npm 2FA
  authorization; verified registry metadata, package integrity, and a clean
  exact-version install. Public `v1.0.2` and `v1` tags now point to the
  verified release commit.
- 2026-08-10: Published `@ahoooooo/reviewready@1.0.3` from merged release
  commit `14147f5` after PR #8 and passing Node 22/24 CI. Verified the public
  registry metadata, package integrity, exact-version install, policy
  validation, and ready fixture check. Public `v1.0.3` and `v1` now point
  to the verified release commit.
- 2026-08-12: After independent adversarial review, prepare the unreleased
  `1.0.4` candidate with offline audit, trusted ingress, AI-workflow taint,
  runtime-boundary, release-metadata, and deterministic-order regression fixes.
  The local gate passes; no commit, push, npm publish, GitHub release, or tag
  movement is authorized by this candidate record.
- 2026-08-10: Harden the v1 evidence boundary: structured requirement keys,
  fenced-Markdown parsing, timestamp-aware latest reviews, commit-status
  evidence, bounded-pagination fail-closed behavior, and runtime/schema parity
  tests. Keep `merge_group` explicitly unsupported until a trustworthy
  per-PR aggregation contract exists.

## 2026-08-11 integration decisions

- Restore the v1 public requirement-key format while keeping structured
  internal identities. Reject literal backslashes in Git paths, retain both
  sides of renames, ignore hidden Markdown evidence, and describe human
  attestation checks as visible checked text only.
- Stage GitHub evidence collection by the triggered requirement types, bound
  reviewer permission concurrency, reduce latest Check Run and legacy status
  identities with a conservative same-name cross-provider aggregate, and bind
  collection to a verified pull-request snapshot with one bounded retry.
- Preserve same-name Check Runs from multiple App identities as both
  app-specific evidence and a latest unqualified aggregate. Keep pending or
  unknown conclusions as non-satisfying normalized evidence so a provider
  success cannot mask ambiguity.
- Record the trusted workflow-root design in
  docs/adr/0001-trusted-workflow-root.md. The normal pull_request sample remains
  advisory until the repository's GitHub workflow and ruleset settings are
  separately protected and verified.
- Reject an upstream pagination page that exceeds its requested page size or
  the cumulative v1 evidence limit; do not trust a provider that violates the
  bounded collection contract.
- Keep the CLI file-reader hard limit invariant for every caller, including
  explicit helper arguments; reject any requested limit above the bounded
  maximum so the helper cannot bypass the CLI trust boundary.
- Require every Check Run to carry the requested immutable head SHA when the
  gateway is called with a commit ref; branch refs remain endpoint-bound.
- Treat GitHub-nullable queued/in-progress timestamps as absent pending
  metadata, but never allow them to produce a successful conclusion.
- Read required GitHub evidence twice with a bounded canonical fingerprint and
  verify the pull-request snapshot after the second read; retry when either
  evidence or metadata changes.
- Treat malformed or non-contiguous pagination Link next URLs as incomplete
  evidence instead of silently accepting a truncated page sequence.
- Never let a Check Run or commit status with invalid timestamp ordering select
  a successful conclusion from the other provider's same-name evidence.
- Preserve raw Markdown syntax while removing invisible content only for
  visibility checks; quoted HTML attributes and named invisible entities are
  treated as untrusted structure.
- Sanitize control characters in all public ReviewReady errors and reject
  ambiguous duplicate CLI path options before reading files.

## 2026-08-11 follow-up phase

The next delivery is intentionally split from readiness:

1. Freeze and test the normalized, offline repository-audit contract. It must
   be read-only, bounded, deterministic, and fail closed; its report and SARIF
   version must remain separate from the readiness schema.
2. Add pure trusted-ingress primitives for raw-body HMAC validation, atomic
   replay claims, freshness, and SHA/policy/workflow binding. Do not add a
   server, credentials, or deployment until those interfaces are reviewed.
3. Add bounded static AI-workflow source/prompt/sink analysis with fixtures and
   distinct prompt-injection, code-execution, capability, and provenance
   findings. Never execute workflows or invoke an LLM.
4. Dogfood the audit against a checked-in normalized ReviewReady fixture, then
   update architecture, security, CLI, package, and release evidence only after
   focused tests and the complete gate pass.

The pure/live GitHub settings collector, bounded GitHub App authentication
helpers, and webhook ingress/result-binding contracts are now implemented as
read-only library/CLI surfaces. A production HTTP service, durable replay
storage, organization policy, secret rotation, and merge-gate deployment remain
deferred until their permissions, freshness, privacy, and operational contracts
are separately demonstrated.

The audit contract was tightened during adversarial review: active rulesets
must carry a bounded branch/ref scope that covers the evaluated default branch;
all normalized workflows are checked for trusted-root protection to reduce
same-name check spoofing; check/status/deployment write permissions are
reported; and workflow finding caps emit an explicit incomplete result instead
of silently dropping findings.

## 2026-08-12 continuation

- Add the live collector with immutable base-SHA policy/workflow reads, a second
  branch consistency read, explicit trusted/protected workflow roots, and
  bounded API response/pagination/retry/concurrency/deadline limits.
- Add bounded GitHub App JWT/installation-token helpers and exact raw-body
  webhook ingress. Durable stores must atomically claim delivery and body
  replay namespaces; no in-memory fallback is provided.
- Restore the original v1 readiness `summary` JSON text and keep newer wording
  in presentation renderers only. Add the separate readiness-result schema.
- Dogfood the current repository workflow through a deterministic audit fixture;
  the fixture intentionally reports trusted-root posture findings rather than
  pretending the repository passes.
- Add exact npm tarball audit, SHA-512 recording, clean-room install/CLI smoke,
  and a safe pull-request/manual release-preflight workflow. Persistent npm
  publishing, GitHub release/tag movement, and external protection setup remain
  deferred until their permissions and release authority are explicitly
  verified.

## Release evidence

Historical v1.0.0 release-candidate evidence is recorded in
`docs/release-evidence-v1.md`. The GitHub repository and Action remain public at
`ahoooooooo/reviewready`; `@ahoooooo/reviewready@1.0.3` is the current public
package and npm `latest` version. Recovery evidence for the previous package
is recorded in `docs/release-evidence-v1.0.2.md`.

The evidence covers the full local gate, coverage, production Action bundle,
`npm pack --dry-run`, a tarball clean install, CLI smoke tests, GitHub event
fixtures, dependency audit, and documented limitations. The v1.0.3 patch
release evidence is recorded in `docs/release-evidence-v1.0.3.md`.

The current working tree is a local, unreleased `1.0.4` candidate. It has not
been committed, pushed, published to npm, or used to move a GitHub tag. Its
local audit-core evidence is recorded in
`docs/release-evidence-v1.0.4.md`; public v1.0.3 remains the latest released
version until an explicit release decision.
